This folder is reserved for your personal images.

- The hero portrait uses `images/me.png` (you can replace it with your own image).
- To use another file, put it in this folder and update the `src` in `index.html`.

