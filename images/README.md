This folder is reserved for your personal images.

- Put your main portrait photo here as `me.jpg`.
- In `index.html`, the hero portrait uses `images/me.jpg` as the source.
- You can replace `me.jpg` with any other filename, just don't forget to update the `src` attribute in `index.html` accordingly.

